# Install Astro
```
yarn create astro
# ./astro101; empty; typescript=no;
```
The top section is FrontFormatter Section. This section only runs on the serverside.  
Because the default index.astro file executes at build time, Astro default behaviour is Static Site Generation(SSG).
## Dynamic Route

# Server-Side Rendering
## node adapter
