# Open a file
```shell
$ vim /etc/passwd

$ vim
:e /etc/passwd
```

# Save a file
```shell
:w (or) :write
:up (or) :update
:w newfile.txt
:up newfile.txt
:w! newfile.txt
:up! newfile.txt
```

# Close a file
```shell
:x
:wq
ZZ
:q!
:qa
```

# Types of Vim Modes
## Basic Vim Modes

